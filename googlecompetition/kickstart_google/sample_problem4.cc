#include <iostream>
#include <istream>
#include <vector>


using namespace std;

void solution(vector<int> arr, int n, int c){
    string opt[] = {"Impossible", "Blue wins", "Red wins", "Nobody wins" };
}

int main(){
    int N, M;
    cin >> N;
    vector <vector <char>> maze;

    while(N--){
        cin >> M;
        while(M--){
            while(M--){

            }
        }
    }

    return 0;
}